**Label:** `refresh-by-design`, `pipeline`, `docs`
**CLAUDE.md:** §4.1 (oracle is validation-only), §4.3

### Problem
`02` and `03` set `year_tag <- "2023"`, write `rule_scorecards_2023.csv` /
`edmonton_row_labels_2023.csv`, and read the confidential xlsx. This is **correct and must
not be parametrized** — the oracle is a frozen 2023 snapshot, validation runs once, rules
ship already-validated. But the `2023` literal sits in the same code shape as the
`2026` literals from #2, so whoever does the #2 refactor could "helpfully" parametrize it
and break the methodology boundary.

### Fix
Documentation guardrail, not a code change.

### Acceptance
- [ ] One-line header comment in `02` and `03`: `# year_tag is FROZEN — oracle is the 2023 snapshot; do NOT parametrize (CLAUDE.md §4.1).`
- [ ] A line in CLAUDE.md §9 negative rules: "Do not parametrize the oracle year — validation is frozen at 2023."
